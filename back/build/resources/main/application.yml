server:
  port: 8080
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nlp?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root  # 실제 MySQL 사용자 계정
    password: elskvhfh12  # 해당 계정의 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect  # MySQL 8용 Hibernate Dialect
    hibernate:
      ddl-auto: update  # 개발 시 update, 운영 시 none 권장
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  # 선택: 한글 저장 문제 예방
  sql:
    init:
      encoding: UTF-8


  # ✅ 이메일 (네이버 SMTP 사용)
  mail:
    host: smtp.naver.com               # 네이버 SMTP 서버
    port: 465                          # SSL 포트
    username: nskfn02@naver.com       # 네이버 이메일
    password: QLGVFX36FNC3        # 네이버 앱 비밀번호
    properties:
      mail:
        smtp:
          auth: true                   # SMTP 인증 활성화
          ssl:
            enable: true               # SSL 활성화
            trust: smtp.naver.com      # SSL 신뢰할 SMTP 서버

  # ✅ Redis 설정
  data:
    redis:
      host: localhost
      port: 6379

  # uploads 경로 정적 리소스 인식
  web:
    resources:
      static-locations: file:C:/FinalProject/uploads
      add-mappings: true

  # 첨부파일 크기 제한
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  ai:
    openai:
      api-key: AIzaSyAnMziLv24BNczctq-J1k-fjH8sqalPKIk

# ✅ 로깅 설정 (디버깅을 위한 로그 레벨)
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

livekit:
  api-key: 878de0038dbed61ed7daab9872fed4e2017e1a37a0b12038a1d62573b64cb4bc
  api-secret: 878de0038dbed61ed7daab9872fed4e2017e1a37a0b12038a1d62573b64cb4bc
  ws-url: ws://localhost:7880
  ttl-minutes: 60
